# Yongkang Zou Portfolio

一个具有赛博朋克风格的个人作品集网站，使用 Next.js 15 和 React 19 构建。

## 🚀 快速开始

### 安装依赖

```bash
npm install --legacy-peer-deps
```

### 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看网站。

### 构建生产版本

```bash
npm run build
npm start
```

## 📁 项目结构

```
.
├── app/                      # Next.js 应用路由
│   ├── page.tsx             # 首页 - 带有赛博朋克开场动画
│   ├── about/               # 关于页面
│   ├── projects/            # 项目展示页面
│   ├── music/               # 音乐页面
│   ├── writing/             # 写作页面
│   ├── contact/             # 联系页面
│   ├── layout.tsx           # 全局布局组件
│   └── globals.css          # 全局样式
│
├── components/              # React 组件
│   ├── cyberpunk-intro.tsx      # 赛博朋克开场动画组件
│   ├── floating-nav.tsx         # 浮动导航栏
│   ├── bottom-nav.tsx           # 底部导航栏
│   ├── navigation.tsx           # 主导航组件
│   ├── matrix-rain.tsx          # 黑客帝国雨特效
│   ├── floating-particles.tsx   # 浮动粒子效果
│   ├── pixel-stars.tsx          # 像素星空背景
│   ├── audio-visualizer.tsx     # 音频可视化组件
│   ├── netease-music-player.tsx # 网易云音乐播放器
│   ├── project-chatbot.tsx      # 项目聊天机器人组件
│   ├── three-scene.tsx          # Three.js 3D 场景组件
│   ├── GlassSurface.jsx         # 玻璃质感表面组件
│   ├── LetterGlitch.jsx         # 字母故障效果组件
│   ├── SpotlightCard.jsx        # 聚光灯卡片组件
│   ├── sound-provider.tsx       # 音效上下文提供者
│   ├── sound-toggle.tsx         # 音效开关组件
│   ├── theme-provider.tsx       # 主题提供者
│   └── typing-text.tsx          # 打字机文本效果
│
├── lib/                     # 工具函数
│   └── utils.ts            # 通用工具函数
│
├── public/                  # 静态资源
│   ├── images/             # 图片资源
│   ├── icon.svg            # 网站图标
│   └── placeholder-*       # 占位图片
│
├── styles/                  # 额外样式文件
│   └── globals.css         # 全局 CSS
│
├── next.config.mjs         # Next.js 配置
├── tailwind.config.ts      # Tailwind CSS 配置
├── tsconfig.json           # TypeScript 配置
└── components.json         # shadcn/ui 组件配置
```

## 🎨 页面结构

### 首页 (`/`)
- **赛博朋克开场动画**：城市天际线、霓虹灯效果、黑客文本滚动
- **特效组件**：
  - 浮动粒子效果
  - 像素星空背景
  - 黑客帝国雨效果
- **交互元素**：
  - 浮动导航栏
  - 音效开关
  - 主题切换

### 关于页面 (`/about`)
个人简介和背景信息展示

### 项目页面 (`/projects`)
作品集和项目案例展示
- **项目卡片翻页展示**：每个项目包含多页内容
  - 第一页：项目概述、标题、日期、标签
  - 第二页及后续页：Key Features 分页展示（每页最多 2 个特性，显示 "Key Features (x/y):" 格式）
  - 最后一页：项目链接（GitHub 代码、在线演示）
- **项目时间标注**：每个项目卡片的所有页面都显示项目日期
- **交互式卡片**：支持翻页浏览、悬停效果

### 音乐页面 (`/music`)
- 网易云音乐播放器集成
- 音频可视化效果

### 写作页面 (`/writing`)
博客文章和写作内容

### 联系页面 (`/contact`)
联系方式和社交媒体链接

## 🛠 技术栈

- **框架**: Next.js 15.2.4 (App Router)
- **UI**: React 19.2.0
- **样式**: Tailwind CSS + CSS Modules
- **组件库**: Radix UI + shadcn/ui
- **动画**: Framer Motion
- **图标**: Lucide React
- **类型**: TypeScript

## 🎭 主要特性

1. **赛博朋克主题**: 霓虹灯效果、科技感动画
2. **响应式设计**: 适配各种屏幕尺寸
3. **暗色模式**: 支持主题切换
4. **音效系统**: 交互音效和背景音乐
5. **特效丰富**: 多种视觉特效增强体验
6. **项目展示优化**: 
   - 项目卡片多页翻页展示
   - Key Features 智能分页（每页最多 2 个特性）
   - 项目时间标注显示
7. **3D 效果**: Three.js 场景和玻璃质感组件
8. **性能优化**: 使用 Next.js 15 的最新特性

## 📝 开发说明

### 添加新页面

在 `app/` 目录下创建新文件夹和 `page.tsx` 文件：

```tsx
export default function NewPage() {
  return <div>New Page Content</div>
}
```

### 创建新组件

在 `components/` 目录下创建新的 `.tsx` 文件：

```tsx
export function NewComponent() {
  return <div>Component Content</div>
}
```

### 修改样式

- 全局样式: 编辑 `app/globals.css` 或 `styles/globals.css`
- Tailwind 配置: 编辑 `tailwind.config.ts`
- 组件样式: 使用 Tailwind 类名或 CSS Modules

## 🐛 已知问题

- **Hydration Mismatch 警告**: 由于使用 `Math.random()` 生成动画参数导致服务器端和客户端渲染不一致，不影响功能
- **依赖冲突**: 使用 `--legacy-peer-deps` 标志安装依赖以解决 React 19 的对等依赖问题

## 📄 License

MIT